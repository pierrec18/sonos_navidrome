# Sonos Navidrome SMAPI - Configuration Environment
# Copiez ce fichier vers .env et adaptez les valeurs

# Port du service (défaut: 4000)
PORT=4000

# Répertoire des données utilisateur (liens OAuth)
DATA_DIR=./data

# Clé de chiffrement pour stocker les credentials (OBLIGATOIRE - 32 caractères)
# Générez avec: openssl rand -hex 16
ENCRYPTION_KEY=your-32-character-encryption-key-here

# TTL des tokens OAuth en secondes (défaut: 1 an)
TOKEN_TTL_SECONDS=31536000

# URLs de redirection autorisées (optionnel, séparées par virgules)
# Si non défini, toutes les URLs sont autorisées (dev uniquement)
ALLOWED_REDIRECT_PREFIXES=https://your-domain.com,https://localhost

# Credentials OAuth pour validation client (optionnel mais recommandé)
OAUTH_CLIENT_ID=sonos-navidrome
OAUTH_CLIENT_SECRET=your-secret-key-here

# Configuration Node.js
NODE_ENV=production

# Logging (optionnel)
LOG_LEVEL=info
